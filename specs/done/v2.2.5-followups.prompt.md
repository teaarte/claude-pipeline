# Launcher prompt — v2.2.5 follow-ups

Paste this into a fresh Claude Code session in the `claude-pipeline` repo.

---

## PROMPT START

You are executing the **v2.2.5-followups** fix bundle: 13 HIGH + 16 MEDIUM + 6 LOW items surfaced by full-codebase code review post-v2.2.5. Two of the HIGH items are regressions I introduced in v2.2.5 itself; the rest are pre-existing bugs. Real-task verification of v2.2.5 was deliberately paused until at least the HIGH set lands.

### Source of truth

Read **`specs/v2.2.5-followups.md`** in full before writing any code. Every fix has its location, symptom, chosen approach (decided per-item, not your call to re-litigate), and a test sketch. Decisions you would otherwise have to make have been pre-resolved — follow the document literally.

In particular, these decisions are already made; do not deviate:
- **H7 (guard.sh)** — inspect `bash -c` body for inner mutator, don't drop the pattern.
- **H13 (fix-task-id)** — atomic rewrite of findings.jsonl, not hard-refuse.
- **M8 (dead scratch flags)** — delete them (v2.3+ plan doesn't read them).
- **M12 (refs missing sections)** — drop the section-name rule in 5 reviewer agents; do NOT add prose to 12 ref files. File a Q-item for future ref-content enrichment phase.

Also read for background context:
- `specs/phases/v2.2.5-bundle-foundation.md` — what just shipped.
- `mcp/src/driver/types/plugin.ts` — current contracts.
- `mcp/src/driver/bundles/code/steps/index.ts` — the 1000+ line hot file most HIGH fixes touch.

### Hard rules

- **Branch:** `v2.2.5-followups` off whatever the current main/branch tip is. Verify with `git status && git log --oneline -5` first.
- **One commit per group** per the "Suggested grouping" section of the followups document (10 commits total: C1-C10). Conventional commit subjects.
- **Tests with code** — every HIGH fix ships ≥ 1 new or extended test (test sketches are in the document). MEDIUM/LOW: only test if there's non-trivial behavior to lock down.
- **Run before every commit:**
  ```bash
  pnpm --filter "@claude-pipeline/mcp" typecheck
  pnpm --filter "@claude-pipeline/mcp" test
  pnpm --filter "@claude-pipeline/mcp" smoke
  pnpm --filter "@claude-pipeline/mcp" smoke:orchestrator
  ```
  All green per commit. Baseline test count is **396**; target is **≥ 416** at end (≥ 20 new tests).
- **Grep purity gate** — `grep -rEi "planner|implementer|logic-reviewer|gate-[012]|simple-flow|medium-flow|complex-flow" mcp/src/driver/core/` must return zero per commit.
- **No backward-compat shims.** Replace and delete, don't wrap. Apply the principle in `/Users/teaarte/.claude/projects/-Users-teaarte-Programming-internal-claude-pipeline/memory/feedback_no_backcompat.md`.
- **No push.** Local commits only.
- **Standard footer:**
  ```
  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  ```
- **If a fix turns out larger than expected or you hit a blocker** — stop, commit the partial work with a clear status comment, document the blocker in your hand-back. Don't half-finish; don't bypass.

### Execution order

Follow the document's "Suggested grouping" section literally:

1. **C1** — H1 (continue-task zod regression) — closes Item 8 on real path.
2. **C2** — H2 (team_knowledge fan-out regression) — closes Item 7 on real path.
3. **C3** — H3 + H4 (stack-detect fixes).
4. **C4** — H5 + H6 + L6 + M11 + M12 (agent templates / refs frontmatter / rule loosening).
5. **C5** — H7 (guard.sh hardening).
6. **C6** — H8 + H9 (mcp-client-manager error handling + timeout cleanup).
7. **C7** — H10 + H11 + H12 (lock races + state-io correctness).
8. **C8** — H13 (fix-task-id atomic findings rewrite).
9. **C9** — M1-M10 + M13-M16 (drift cleanup).
10. **C10** — L1-L5 (low items).

After C4, the codebase is safe enough for real-task verification of v2.2.5 even if C5-C10 slip. C1-C4 are the gate.

### Hand-back

Print a summary:
- 10 commit SHAs (or N < 10 if any blocked, with explanation).
- Test count before / after.
- Files changed / added / removed counts.
- Q-items closed (this bundle doesn't close existing Q-items but may file new ones — Q62+ for the deferred ref-content phase, others as you discover them).
- Roadmap status — update `open-backlog.md` if any new Q-items were filed.
- Validation entry — new `validation/closed-tasks/2026-05-19-v2.2.5-followups.md` summarising what shipped + what slipped.
- Recommendation for human's next step:
  - Real-task verification of v2.2.5 on s3-panel OR wandr-be — now safe to run with C1-C4 landed.
  - If any C5-C10 commits slipped, list them so the human can decide whether to merge anyway or hold.

End the session. Do not start real-task validation from this session — leave that for a fresh session.

## PROMPT END
