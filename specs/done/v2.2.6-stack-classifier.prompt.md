# Launcher prompt — v2.2.6 stack-classifier + critical workflow fixes

Paste this into a fresh Claude Code session in the `claude-pipeline` repo.

---

## PROMPT START

You are executing **v2.2.6**: stack-classifier architectural shift + three workflow-critical fixes from the frontend-core 2026-05-18 real-task run. 8 commits, ~3 days work.

This phase replaces ~200 lines of per-language regex in `stack-detect.ts` with a YAML candidate registry (C# / Svelte / Elixir / Dart support without TS edits), extends the classifier-agent output schema with `stack` + `change_kind` fields (schema only — auto-spawn lands in v2.2.7), adds a CLAUDE.md `<!-- validation-commands -->` marker convention, fixes canonical task_id propagation, fixes the Q63 "phase-force-final fires on every successful run" workflow leak, and adds Q64 cross-session ownership safety to `pipeline-stop.sh` + MCP tools.

### Source of truth

Read **`specs/phases/v2.2.6-stack-classifier.md`** in full before writing any code. Every item has its location, symptom, chosen approach (decided per-item, not your call to re-litigate), and a test sketch. Decisions you would otherwise have to make have been pre-resolved — follow the document literally.

In particular, these decisions are already made; do not deviate:
- **Item 1 stack-candidates.yaml** — single YAML file, Zod-validated on load. Adding new language = edit YAML, no TS change.
- **Item 2 stack-detect.ts** — two functions only: `gatherStackSignals(projectDir)` + `resolveStack(signals, candidates)`. Zero per-language conditional branches in TS.
- **Item 3 classifier schema extension** — `stack` + `change_kind` fields added to `classifier-output.schema.json`. Schema only. **Auto-spawn DEFERRED to v2.2.7 Item 1.** Do not auto-spawn classifier in v2.2.6.
- **Item 4 CLAUDE.md marker** — `<!-- validation-commands -->` block parses first; English header is fallback. Same shape as `<!-- antipattern -->` from v2.2.5.
- **Item 6 task_id propagation** — spawn-context "Canonical identifiers" block + agent-template constraint + defensive runtime check in `record_agent_run` with audit class `task_id-rewrite`. Rewrite (don't reject) when mismatch is a known semantic-vs-canonical surface form.
- **Item 7 auto-close (Q63)** — auto-close `validation` on `acceptance: PASS` + zero open_spawns; auto-close `final` in `pipeline_finish` on clean accepted verdict with all prior phases closed. No `force=true` on auto-close path. Force path stays intact for Q54 genuine recovery.
- **Item 8 cross-session ownership (Q64)** — `state.owner_id` generic field. Env-var chain: `CLAUDE_PIPELINE_OWNER_ID` → `CLAUDE_SESSION_ID` → `SESSION_ID` → null. `pipeline-stop.sh` reads CC session_id from stdin and compares. `pipeline_finish` / `pipeline_abandon` gain `OWNER_MISMATCH` error code with `force_cross_owner: true` override (audited as `pipeline_violation: "cross-owner-finalize"`). **Critical: CC-specific code lives ONLY in `hooks/pipeline-stop.sh`. Everything else stays platform-agnostic.**

Also read for background context:
- `specs/phases/v2.2.5-bundle-foundation.md` — what just shipped (Item 9 substrate is the foundation for classifier schema extension here).
- `specs/phases/v2.2.7-classifier-portability.md` — what comes next; v2.2.6 Item 6 (task_id) is a strict prereq for v2.2.7 Item 1.
- `validation/closed-tasks/2026-05-18-frontend-core-phase07-step1.md` — real-task data that motivated Items 6-8.

### Hard rules

- **Branch:** `v2.2.6-stack-classifier` off `main` after `git pull`. Verify `git status` clean first.
- **One commit per group (8 commits total: C1-C8)**, in the order listed in the spec's "Implementation order" section. Conventional commit subjects.
- **C6 (task_id) lands BEFORE C7+C8** — this is non-negotiable. v2.2.7 Item 1 (classifier auto-spawn) is a downstream consumer that needs canonical task_id propagation in place.
- **Tests with code** — every commit ships ≥ 1 new or extended test. Target: baseline (post-v2.2.5) + ≥ 15 new tests.
- **Run before every commit:**
  ```bash
  pnpm --filter "@claude-pipeline/mcp" typecheck
  pnpm --filter "@claude-pipeline/mcp" test
  pnpm --filter "@claude-pipeline/mcp" smoke
  pnpm --filter "@claude-pipeline/mcp" smoke:orchestrator
  ```
  All green per commit.
- **Grep purity gate** — `grep -rEi "planner|implementer|logic-reviewer|gate-[012]|simple-flow|medium-flow|complex-flow" mcp/src/driver/core/` must return zero per commit.
- **No backward-compat shims.** Solo-user codebase. Apply the principle in `/Users/teaarte/.claude/projects/-Users-teaarte-Programming-internal-claude-pipeline/memory/feedback_no_backcompat.md`. Specifically: when refactoring `stack-detect.ts` (C3), delete the old per-language code — don't leave it as a "deprecated path". Schema bumps are strict, not enum'd.
- **CC-binding boundary for Q64 (C8)** — the only place where Claude Code session_id is referenced directly is `hooks/pipeline-stop.sh` (because Stop is CC's plugin event). Everywhere else uses the generic `state.owner_id` field. Verify with `grep -rE "CLAUDE_SESSION_ID|claude.code.task" mcp/src/` returning zero matches after C8.
- **No push.** Local commits only.
- **Standard footer:**
  ```
  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  ```
- **If a fix turns out larger than expected or you hit a blocker** — stop, commit the partial work with a clear status comment, document the blocker in your hand-back. Don't half-finish; don't bypass.

### Execution order

Follow the document's "Implementation order" section literally:

1. **C1** — Doctrine update (`specs/product-vision.md` line 257 reclassifies stack-detect Category 1 → Category 3) + DB-direction doc in `specs/phases/far-future.md`. Doc-only commit. ~30 min.
2. **C2** — `templates/stack-candidates.yaml` + `mcp/src/lib/stack-candidates.ts` loader + Zod validation + tests (≥ 4 languages including 1 new like C#). ~3-4h.
3. **C3** — `mcp/src/driver/bundles/code/decisions/stack-detect.ts` rewrite as candidate-driven. Replace per-language regex with `gatherStackSignals` + `resolveStack`. Tests get C# / Svelte / Elixir / Dart fixtures. **Largest single commit.** ~6-8h.
4. **C4** — Extend `templates/schemas/classifier-output.schema.json` with `stack` + `change_kind` fields + matching prompt section in `agents/classifier.md`. Schema validation tests. ~3h.
5. **C5** — `<!-- validation-commands -->` marker parser in `stack-detect.ts` (or extracted helper) + `mcp/README.md` authoring section + tests. ~3h.
6. **C6** — Canonical task_id propagation. Spawn-context block + agent-template edits + defensive runtime check + audit class. **Land BEFORE C7+C8.** ~3-4h.
7. **C7** — Q63 auto-close: `mcp/src/tools/record-agent-run.ts` (validation auto-close on acceptance:PASS) + `mcp/src/tools/finish.ts` (final auto-close on clean success). Tests cover happy + regression paths. ~3-4h.
8. **C8** — Q64 cross-session: `state.owner_id` field in schema + env-var read in `pipelineRunTask` + `hooks/pipeline-stop.sh` owner-comparison logic + `pipeline_finish` / `pipeline_abandon` `OWNER_MISMATCH` check + bash + TS tests. ~4h.

After C5, the codebase has full classifier-substrate ready for v2.2.7. After C8, the codebase is safe for multi-window workflow + clean-success metrics.

### Hand-back

Print a summary:
- 8 commit SHAs (or N < 8 if any blocked, with explanation).
- Test count before / after.
- Files changed / added / removed counts.
- Q-items closed (Q63 + Q64; Items 1-5 are architectural shifts, not Q-items per se).
- Any NEW Q-items surfaced during execution — file in `specs/open-backlog.md` with next available number (currently Q67).
- Update `specs/open-backlog.md` to move Q63 / Q64 rows to `specs/closed-q-items.md` under new "v2.2.6-stack-classifier" section, each with commit SHA.
- Roadmap status — confirm `specs/phases/v2.2.6-stack-classifier.md` acceptance criteria all ticked. If anything slipped, note it in the hand-back.
- Validation entry — new `validation/closed-tasks/<date>-v2.2.6-shipped.md` summarising what shipped + recommended next-step (real-task validation on 2-3 different change_kinds before starting v2.2.7).
- Recommendation for human's next step:
  - Real-task validation: run `/task` on 2-3 different change_kinds (type-only, logic, ui) on frontend-core OR wandr-be. Confirm canonical task_id in findings, no `phase-force-final` on success runs, cross-session hook behaves correctly (test with 2 windows).
  - After validation: v2.2.7 (classifier auto-spawn + reviewer selectivity + portability foundations Q65/Q66).

End the session. Do NOT run `/task` from this session — human will start fresh session for real-task validation post-merge.

## PROMPT END
