# Open backlog

Active Q-items not yet shipped. Updated when new bugs surface from real-task validation OR existing items change status (e.g., Q41 partial → fully shipped when v2.3 daemon activates LLM path).

For closed items, see [`closed-q-items.md`](closed-q-items.md). For pipeline phase plans, see [`phases/`](phases/).

## Currently open

| Q | Severity | Status | Notes |
|---|---|---|---|
| Q52 | 🟡 MEDIUM | open | **Partial-commit during `pipeline_continue_task agents-results` batch with mixed valid/invalid headers.** Real-task frontend-core 18:06:36: batch of 4 reviewer results → style-reviewer's findings had `severity: "non-blocking"` (not in enum) + missing `agent` field → batch returned error, but logic/challenger/performance were silently committed. Retry hit INV_012 (already-recorded ar-ids). Recovery method-by-trial. **Fix:** make `agents-results` atomic — either all results commit or none; per-result error report so the caller knows which subset to retry. ~2h. | `mcp/src/tools/record-agent-run.ts` (batch path). |
| Q53 | 🟢 LOW | open | **Stale `pending_spawns` in `driver-state.json` after Q52-recovery.** Downstream of Q52. Implementation phase clean in pipeline-state but driver-state retains the orphan ar-ids. Likely fixed for free once Q52 is atomic. | `mcp/src/driver/tools/continue-task.ts` (driver-state cleanup path). |
| Q54 | 🟢 LOW | open | **Workflow gap: gate-2 reject-with-feedback but still accept.** User on frontend-core rejected gate-2 with revision feedback but decided to ship anyway. Path required `pipeline_set_phase_status final completed force=true` then `pipeline_finish verdict=accepted` — recorded as `pipeline_violation: "phase-force-final"`. Design question, not bug. **Possible fix:** dedicated answer `acknowledge-and-ship` for gate-2 that stores feedback for the next task but doesn't block finish. Defer until next time the pattern recurs. | gate-2 protocol + `pipeline_finish`. |
| Q62 | 🟢 LOW | open | **Ref content enrichment phase.** v2.2.5-followups (M12) loosened the reviewer-prompt rule that required `**Red Flags in Diff**` / `**Anti-Patterns**` sections in every ref. ~12 refs under `agents/references/{e2e,perf,test,ui}-*.md` never had those sections, so the rule was silently dead. A future phase should decide whether structured sections, extended frontmatter, or per-stack ref expansion is the right shape, then re-enrich the bare refs accordingly. Not blocking — frontmatter (`tags`, `agent_hints`, `summary`, `when_to_load`) already signals relevance to reviewers. | `agents/references/`. |
| Q65 | 🟡 MEDIUM | **scheduled v2.2.7 Item 4** | **Generic shuttle response shape (`claude_code_task` → `spawn_request`).** `mcp/src/driver/types/shuttle.ts` defines `ClaudeCodeTaskSpec` + `DriverResponse.claude_code_task` field. Both are direct copies of Claude Code Task-tool params — any non-CC consumer (Cursor MCP, future HTTP API client, Codex/Gemini MCP integration) can't proxy the spawn correctly. **Fix:** rename to `SpawnRequest { runner_hint, description, prompt, model?, extras }`. `runner_hint: "claude-code-task" \| "anthropic-sdk" \| "openai-sdk" \| ...` declares HOW to invoke. CC-specific fields (`subagent_type`) move to `extras`. ShuttleSpawnProvider sets `runner_hint: "claude-code-task"`; AnthropicSdkSpawnProvider (v2.3.4) sets `"anthropic-sdk"`. Skill markdown reads `spawn_request.runner_hint` to know which client-side primitive to invoke. No backward-compat shim — clean rename. ~1 day. |
| Q66 | 🟢 LOW | **scheduled v2.2.7 Item 5** | **Configurable project subdirectory.** `mcp/src/lib/paths.ts` hardcodes `.claude/` as the project's pipeline working dir — Claude Code convention. For daemon / non-CC users this naming is misleading branding. **Fix:** read from env var `CLAUDE_PIPELINE_PROJECT_SUBDIR` with default `.claude` (existing CC projects unchanged). Helpers `claudeDir()` etc. use the variable. Documented in `mcp/README.md`. No migration logic — solo-user, opt-in via env. ~2-3h. |
| Q67 | 🟡 MEDIUM | **scheduled v2.2.7 Item 6** | **Planning-phase reviewers spawn sequentially (no fan-out).** User-observed during real-task runs: `plan-grounding-check` and `logic-reviewer` at planning gate-1 fire as two separate FSM steps (`PLAN_GROUNDING` → shuttle → result → `PLAN_REVIEW` → shuttle → result), each via `spawnOne`. Implementation phase already uses `spawnAgentsParallel` (Q9 fan-out) for the 5-reviewer batch — the primitive exists, just isn't applied at planning. **Cost:** ~30-60s of wall time per medium/complex task (extra shuttle round-trip + serialized LLM execution where parallel would let CC's Task tool drive both subagents concurrently). **Fix:** merge `PLAN_GROUNDING` + `PLAN_REVIEW` into one `plan-review` step (or rename to `plan-fanout`) that emits `spawnAgentsParallel` over `[plan-grounding-check, logic-reviewer]` (and optionally `challenger-reviewer` for cross-perspective). SIMPLE flow unchanged (grounding alone). Update `flows/index.ts` step lists for MEDIUM + COMPLEX. ~3-4h including FSM-test updates. **Where:** `mcp/src/driver/bundles/code/steps/index.ts` (PLAN_GROUNDING + PLAN_REVIEW → single PLAN_REVIEW), `flows/index.ts` (drop `plan-grounding` from MEDIUM/COMPLEX step lists). |
| Q68 | 🔴 HIGH | **scheduled v2.2.7 Item 7** | **`acceptance: PASS` does not gate on open blocking findings from impl-phase reviewers.** Real-task evidence (frontend-core 2026-05-19 task `t-2026-05-19-implementhybridexecu`): style-reviewer at impl iteration=2 emitted 3 blocking prettier violations on new/modified files; acceptance subsequently returned `PASS` because its own `pnpm test/lint/build` call ran against project scope where 17 pre-existing prettier failures are deliberately excluded — so the new-file violations were invisible. Without the user's manual catch at Gate 2, the task would have shipped with blocking findings open. **Consequence:** the workflow value of impl-phase reviewers (Q9 fan-out) silently nullified whenever acceptance happens to PASS on a stale or scoped baseline. **Fix:** before emitting `verdict: PASS`, the acceptance agent (or a post-record invariant) reads `state.reviewer_verdicts[]` and checks for any open `severity: blocking` findings from impl-phase reviewers on the latest iteration. If present → downgrade to `FAIL` with a `summary_line` enumerating the blockers. Two implementation paths: (a) prompt-side instruction in `agents/acceptance.md`; (b) new invariant `INV_013: acceptance.verdict=PASS ⇒ no open blocking findings in implementation phase`. Both can land. ~3-4h. **Where:** `agents/acceptance.md` + `mcp/src/lib/invariants.ts`. |
| Q69 | 🟡 MEDIUM | **scheduled v2.2.7 Item 8** | **Auto-derived gate-1 feedback (one-keypress apply).** When logic-reviewer returns `REQUEST_CHANGES` with N findings, the user currently has to type each `summary + suggested_fix` into the gate-1-reject message by hand (real-task frontend-core 2026-05-19: 7-bullet revision text manually paraphrasing 5 findings). The findings are already structured with `summary` + `suggested_fix` + `severity`. **Fix:** when constructing the gate-1 message (`bundles/code/gates/index.ts` GATE_1.message), aggregate the latest planning-phase reviewer findings into a pre-filled "Suggested revision" block sorted by severity. New gate-1 protocol: `1/auto-apply-all` (replan with auto-derived feedback, same as today's user-typed reject) | `2/accept-anyway` (open findings carry forward to impl review — today's accept behavior) | `3/edit <message>` (mutate the auto-derived text before sending) | `4/reject <message>` (free-text reject — today's behavior). Doesn't change FSM shape, just enriches the gate prompt + adds the auto-apply path to the user-answer enum. ~4-6h including UserAnswer schema bump + 4-5 tests. **Where:** `bundles/code/gates/index.ts` (GATE_1.message + GATE_1.validate_response), `agents/classifier-output.schema.json` extension (no — UserAnswer protocol), `commands/task.md` skill parser. |
| Q70 | 🟢 LOW | **scheduled v2.2.7 Item 9 (opt-in)** | **Auto-replan loop on REQUEST_CHANGES (capped, opt-in).** Extension of Q69. Config flag `auto_replan_on_blocking_max: 0 \| 1 \| 2` in `pipeline.config.json` (default 0 = today's behavior). When ≥1: on `logic-reviewer.verdict === REQUEST_CHANGES` at planning, synthesize the Q69 auto-derived feedback as a gate-1-reject internally (no human pause), re-spawn planner with that feedback, re-run plan-review fan-out. Stop after `max` iterations OR when verdict flips to APPROVE → fall through to manual Gate 1. **Why capped:** real-task observation 2026-05-19 — gate-1 reject "add `__mfRuntime` seam for AC-8 integration test" prompted planner to add `BootstrapOverrides` DI seam which challenger-reviewer subsequently called "over-engineered" + T-37 "theatre test". Unlimited auto-replan would scale this pattern. Cap forces human review after N attempts. Depends on Q69 landing first. ~3-4h. **Where:** `bundles/code/steps/index.ts` GATE_1_STEP + new auto-replan path. |
| Q71 | 🟡 MEDIUM | **scheduled v2.2.7 Item 10** | **Gate messages embed `state.task` raw (~10 KB on long prompts).** Real-task evidence (frontend-core 2026-05-19): gate-0 and gate-1 message bodies contained the full 10-KB task description verbatim — user had to scroll through their own input to find the `Reply 1/accept or 2/reject` prompt. `bundles/code/gates/index.ts:16` (gate-0) and `:27` (gate-1) interpolate `state.task` directly. **Fix:** prefer `state.task_short` when populated by classifier (v2.2.7 D1 / Item 1); fall back to `state.task.split('\n')[0].slice(0, 80)` truncation when null. Gate-2 message is already constant — no change needed. ~1h. **Where:** `bundles/code/gates/index.ts:16,27` + small helper. |
| Q72 | 🟡 MEDIUM | **scheduled v2.2.7 Item 11** | **`state.owner_id` silently `null` in production — Q64 protection inactive OOB.** Real-task evidence (frontend-core 2026-05-19): `state.owner_id: null` after `pipelineRunTask` ran. None of `CLAUDE_PIPELINE_OWNER_ID` / `CLAUDE_SESSION_ID` / `SESSION_ID` env vars were visible to the MCP server process. Root cause: CC's `~/.claude.json` mcpServers entry has `"env": {}` (empty) and CC does NOT forward its own session_id to stdio-child processes automatically. The Q64 fix landed in v2.2.6 but the cross-session OWNER_MISMATCH check on `pipeline_finish` / `pipeline_abandon` is silently bypassed (`ownerCheck` returns `no-owner-recorded`). Stop hook still works (it reads session_id from stdin payload, not env). **Fix:** (a) document required CC config in `mcp/README.md` — users need `"env": {"CLAUDE_PIPELINE_OWNER_ID": "${CLAUDE_SESSION_ID}"}` if CC supports env-substitution, OR wrapper script that exports it; (b) emit a one-time `audit` warning `error_class: "owner-id-unset"` on `pipelineRunTask` so the gap is visible in metrics. ~2-3h. **Where:** `mcp/README.md` install section + `mcp/src/driver/tools/run-task.ts` audit on null. |
| Q73 | 🟢 LOW | **scheduled v2.2.7 Item 12** | **`summary_line > 100 chars` retries on reviewer output (audit noise).** Real-task evidence: 2 schema-validation errors during frontend-core 2026-05-19 run, both `/summary_line: must NOT have more than 100 characters`. Templates explicitly say "summary_line ≤ 100 chars" in Output constraints, but agents emit longer strings on complex tasks — system self-corrects via lenient-parse retry (`error_class: "schema-validation"`). **Options:** (a) raise the schema cap to 150 chars (cheap, easy, doesn't hurt downstream); (b) move the constraint up to the prompt's top section so agents see it before composing; (c) post-process by truncating with ellipsis instead of rejecting. Pick one. ~1-2h. **Where:** `templates/schemas/reviewer-output.schema.json` + `validator-output.schema.json` + 13 reviewer/validator agent prompts. |
| Q74 | 🔴 CRITICAL | **scheduled v2.2.7 Item 13** | **Gate-2 reject is silently swallowed — verdict defaults to "accepted".** Real-task evidence (frontend-core 2026-05-19 task `t-2026-05-19-implementhybridexecu`): user replied `2/reject <prettier-fix-feedback>` at Gate 2; metric row landed with `gate2_revisions: 1` (reject counted) AND `verdict: "accepted"` (FSM finalized as if accepted). The task shipped through `pipeline_done_cleanup` with the reject silently dropped on the floor. **Root cause:** `gateStep` resume path in `bundles/code/steps/index.ts:258-261` returns `{type: "advance"}` regardless of whether the decision is accept or reject — `mirrorGateDecision` writes `gate2.status = "rejected"` to pipeline-state but does NOT alter FSM control flow. Then `FINALIZE.run` (line 523) does `state.verdict = state.verdict ?? "accepted"` — driver-state's verdict is null → defaults to "accepted" → `complete` shuttle → `/done` ships verdict=accepted. **Consequence:** user's primary mechanism for "send this back for fixes at gate-2" is non-functional. The reviewer fan-out, acceptance check, gate-2 prompt — none of them can stop a bad task from shipping if the user's reject is ignored at the FSM layer. This is the MOST IMPORTANT bug surfaced from v2.2.6 real-task validation. **Fix (two layers):** (1) On gate-2 reject resume, FSM must EITHER (a) loop back to implementation phase for revision (mirror gate-1 reject pattern: increment gate2_revisions, re-run impl + review fan-out), OR (b) finalize with `verdict: "rejected"` if user explicitly wants to abandon. The user-answer protocol needs to express which: extend `UserAnswer` shape with a `reject_intent: "revise" \| "abandon"` field, OR introduce two new gate-2 decisions. (2) Remove the `?? "accepted"` default in `FINALIZE.run`: if `state.verdict` is null at FINALIZE, the FSM is in an inconsistent state — throw rather than silently default. **Lands FIRST in v2.2.7, before everything else.** Effort: ~4-6h including UserAnswer schema extension + FSM-test additions. **Where:** `bundles/code/steps/index.ts` (gateStep + FINALIZE), `bundles/code/gates/index.ts` (parseDecision + GATE_2.message), `mcp/src/driver/types/plugin.ts` (UserAnswer shape), `commands/task.md` skill parser, `commands/done.md`. |

## Deferred (no trigger to fix yet)

| Q | Severity | Why deferred | Trigger to activate |
|---|---|---|---|
| Q38 | 🟢 LOW | Terminal-tab auto-rename via OSC-0 — Claude Code Bash tool subprocess has no TTY; can't reach user terminal. | Solved natively by v2.3 Web UI (browser tabs instead of terminal escape codes). No fix needed in pipeline. |

## Code-quality follow-ups (v2.2-code-polish bundle, separate)

Surfaced by architecture review post-v2 ship. Not validation-driven (no real-task data backs them). Bundle them whenever convenient — these don't unblock anything user-visible.

| Q | Severity | Effort | What |
|---|---|---|---|
| Q1 | code-quality | ~1d | Reduce 33 `any` types across `mcp/src/` to typed equivalents. Mostly `(state as any).field` patterns that have proper types if traced. |
| Q2 | code-quality | ~1d | `mcp/src/driver/builtin/steps/index.ts` is a 1000+ line hot file. Split per step-kind (review, gate, spawn, etc.) — each ~150 lines. |
| Q3 | code-quality | ~30min | Reviewer/validator agent output examples are 30-50 lines each, structurally identical across 13 files. Consolidate to single `templates/agent-output-formats.md` reference + per-agent category list. |
| Q4 | code-quality | ~30min | `mcp/src/driver/types/plugin.ts` is the only file >300 lines in types/. Split per contract (one file per plugin interface) for cleaner import boundaries. |
| Q5 | code-quality | ~30min | Add CI threshold for test:source ratio. Currently 76% (343 test : ~450 source). Fail if drops below 60%. |
| Q6 | code-quality | ~1-2h | Consolidate single source of truth for agent output examples (overlaps Q3 — possibly merge). |

**Total v2.2-code-polish bundle effort:** ~3-5 days. Ship when:
- Boredom strikes between feature work
- Before adding any new agent template (saves rework)
- As a "warm-up bundle" before a bigger phase

## Adding new Q-items

When real-task validation surfaces a new bug:

1. Add a row to this file's "Currently open" table with severity (🔴 HIGH / 🟡 MEDIUM / 🟢 LOW) + 1-line summary.
2. Add a per-task entry in [`../validation/closed-tasks/`](../validation/closed-tasks/) (or update existing one if mid-flight).
3. Reference the Q-number in commit messages when fixing.
4. On merge → move row to [`closed-q-items.md`](closed-q-items.md) under the relevant bundle, with commit SHA.

**Severity guide:**
- 🔴 HIGH — blocks further validation / breaks `/done` / corrupts state
- 🟡 MEDIUM — degrades correctness or observability silently
- 🟢 LOW — cosmetic / UX friction / non-blocking

Q-numbers monotonically increase (next: Q75). Numbers are stable across closures — once Q39 was filed, it stays Q39 even after fix.

**Note on Q49 / Q56:** Both reserved-then-superseded. Q49 was "gate user-answer English-only" placeholder — became Q57 (structured gate-answer) as a Category-2 fix per architectural principle. Q56 was reserved during a renumbering pass and never assigned. Both numbers remain unused (do not recycle).
