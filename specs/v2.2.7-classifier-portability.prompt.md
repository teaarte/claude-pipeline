# Launcher prompt — v2.2.7 classifier auto-spawn + reviewer selectivity + 11 real-task fixes

Paste this into a fresh Claude Code session in the `claude-pipeline` repo.

---

## PROMPT START

You are executing **v2.2.7**: classifier-agent activation + reviewer selectivity by change_kind + Q65 generic shuttle shape + Q66 configurable subdir + 7 follow-up Q-items from the v2.2.6 real-task validation runs (frontend-core 2026-05-18 and 2026-05-19). **13 commits, ~7-9 days focused work.** This is the largest single phase since v2.2.5.

This phase activates the LLM-classification cluster that v2.2.5 Item 9 left as substrate, turns on reviewer selectivity to save tokens on type-only diffs, fixes the **critical Q74 gate-2 reject silent-swallow bug** (the single most important fix in the whole bundle), and lays the portability foundations for v2.3 daemon.

### Source of truth

Read **`specs/phases/v2.2.7-classifier-portability.md`** in full before writing any code. Every item has location, symptom, root cause, fix design, and a test sketch. Decisions you would otherwise have to make have been pre-resolved — follow the document literally.

Also read for context:
- `specs/phases/v2.2.6-stack-classifier.md` — direct prerequisite. Classifier schema extension (`stack` + `change_kind` fields) + canonical task_id propagation from C4/C6 are dependencies.
- `validation/closed-tasks/2026-05-18-v2.2.6-shipped.md` — v2.2.6 hand-back.
- `validation/closed-tasks/2026-05-18-frontend-core-phase07-step1.md` — original frontend-core run that surfaced D1-D6.
- `specs/open-backlog.md` — full Q-item descriptions for Q65-Q74 (the items map 1:1 to Items 4-13 of v2.2.7).

### Critical decisions pre-resolved

These are settled — do not re-litigate:

- **Item 13 (Q74) gate-2 reject FSM-control-flow fix** — `UserAnswer` shape extends with `reject_intent: "revise" | "abandon"`. On reject-revise: walk FSM index back to impl phase entry, increment `gate2_revisions`, re-run reviewer fan-out. On reject-abandon: finalize with `verdict: "rejected"`. **Remove the `?? "accepted"` default in `FINALIZE.run`** — null verdict at finalize throws `INV_inconsistent-finalize` instead of silently defaulting. Gate-1 stays simple (today's accept/reject semantics — gate-1 reject is always revise). Skill parser keeps `2`/`reject` as `revise` for gate-2 backward consistency; explicit `abandon` for the verdict-rejected path.

- **Item 7 (Q68) acceptance gate on impl-blockers** — both layers land: (a) prompt-side instruction in `agents/acceptance.md` to query `state.reviewer_verdicts[]` and downgrade `PASS` → `FAIL` if any impl-phase blocking findings are open at the latest iteration; (b) new `INV_013: acceptance.verdict=PASS ⇒ no open severity:blocking findings in implementation`. INV_013 is the load-bearing safety net.

- **Item 1 (D1) classifier auto-spawn** — new step `CLASSIFY_AGENT` (or extend existing `CLASSIFY`) spawns the `classifier` agent in `context` phase. Output populates `state.decisions.{refs_to_load, security_needed, antipattern_rules_applicable, task_short, stack, change_kind}`. Failure mode: malformed output → defaults retain, audit `error_class: "llm-classification-needed"`, pipeline continues. **Test rework:** write `mockClassifierSpawnProvider()` helper FIRST, validate on 2-3 tests, then bulk-apply to the ~14 FSM tests that drive the FSM without a real LLM.

- **Item 2 (D2) reviewer selectivity by change_kind** — `AgentPlugin` gains `relevant_for_change_kinds?: string[]` field. REVIEW step in `bundles/code/steps/index.ts` consults it before spawning. Conservative defaults: when `change_kind` is null OR classifier-output absent → spawn ALL reviewers (today's behavior). Skipped reviewers get audit row `reviewer-skipped`. Depends on D1.

- **Item 4 (D4 / Q65) generic shuttle response shape** — rename `ClaudeCodeTaskSpec` → `SpawnRequest { runner_hint, description, prompt, model?, extras }`. CC-specific fields (`subagent_type`) move to `extras`. `claude_code_task` field in `DriverResponse` renamed to `spawn_request`. Audit log entries gain `runner_hint`. No backward-compat shim — clean cut per solo-user policy.

- **Item 5 (D5 / Q66) configurable subdir** — `mcp/src/lib/paths.ts` reads `process.env.CLAUDE_PIPELINE_PROJECT_SUBDIR ?? ".claude"`. All path helpers use the variable. Documented in `mcp/README.md`. No migration logic.

- **Item 6 (D6 / Q67) planning fan-out** — merge `PLAN_GROUNDING` + `PLAN_REVIEW` into one step. Fan out `[plan-grounding-check, logic-reviewer]` via `spawnAgentsParallel` for MEDIUM + COMPLEX flows. SIMPLE flow keeps standalone `plan-grounding`. Drop `plan-grounding` from MEDIUM + COMPLEX step lists in `flows/index.ts`.

- **Item 8 (D8 / Q69) auto-derived gate feedback** — `GATE_1.message(state)` aggregates latest planning-phase reviewer findings into a pre-filled "Suggested revision" block sorted by severity. `UserAnswer` gains `auto-apply` decision. Skill parser: `1`/`a`/`auto-apply` → auto-derived feedback replans; `2`/`accept`/`accept-anyway`; `3`/`edit <text>`; `4`/`reject <msg>`.

- **Item 9 (D9 / Q70) auto-replan loop** — opt-in via `pipeline.config.json` field `auto_replan_on_blocking_max: 0 | 1 | 2`. Default 0 = today. When ≥1: REQUEST_CHANGES at planning iter=N → use D8 auto-derived feedback as synthetic gate-1-reject → replan up to `max` iterations → fall through to manual Gate 1 if cap reached. Audit each iter as `auto-replan`. Depends on D8.

- **Item 10 (D10 / Q71) gate message truncation** — `bundles/code/gates/index.ts` GATE_0 + GATE_1 message use `shortTask(state)` helper: prefer `state.task_short` (D1 populates), fall back to first-line + 80-char truncation. Gate-2 message constant — no change.

- **Item 11 (D11 / Q72) owner_id env-var docs + audit warning** — `mcp/README.md` documents required CC config (`"env": {"CLAUDE_PIPELINE_OWNER_ID": "${CLAUDE_SESSION_ID}"}`). `pipelineRunTask` emits one-time `audit({error_class: "owner-id-unset", verdict: "ok"})` when `ownerId === null` so the gap is visible in metrics.

- **Item 12 (D12 / Q73) summary_line cap** — raise schema cap to 150 chars in `reviewer-output.schema.json` + `validator-output.schema.json`. Update 13 reviewer/validator agent templates' Output constraints. Recommendation (a) from the spec, not (b) or (c).

- **Item 3 (D3) implementer tech-debt auto-capture** — implementer template instructs writing tech-debt observations to `.claude/issues-found.md` BEFORE final output JSON. Post-impl hook `extract-tech-debt-from-prose` (event: `after-agent-result`, agent: `implementer`) backfills prose-mentions with `<!-- auto-captured -->` marker; idempotent on paragraph hash.

### Hard rules

- **Branch:** `v2.2.7-classifier-portability` off `main` after `git pull`. Verify `git status` clean first.
- **One commit per group (13 commits total: D1-D13)** in the execution order below. Conventional commit subjects.
- **D13 lands FIRST** — Q74 gate-2 reject silent-swallow is foundational. Without it, every other gate-2 fix is built on a dirty floor. Non-negotiable.
- **D7 second** — Q68 silent-ship-with-blockers. HIGH severity, independent of D13.
- **Tests with code** — every commit ships ≥ 1 new or extended test. Target: baseline (post-v2.2.6) + ≥ 18 new tests. v2.2.6 ended at 485 tests.
- **Run before every commit:**
  ```bash
  pnpm --filter "@claude-pipeline/mcp" typecheck
  pnpm --filter "@claude-pipeline/mcp" test
  pnpm --filter "@claude-pipeline/mcp" smoke
  pnpm --filter "@claude-pipeline/mcp" smoke:orchestrator
  ```
  All green per commit.
- **Grep purity gate** — `grep -rEi "planner|implementer|logic-reviewer|gate-[012]|simple-flow|medium-flow|complex-flow" mcp/src/driver/core/` must return zero per commit.
- **D4 special grep** — after D4, `grep -rE "claude_code_task" mcp/src/` MUST return zero matches. Skill markdown updates land in the SAME commit as the type rename so /task workflow stays consistent.
- **No backward-compat shims.** Solo-user codebase. UserAnswer shape extends cleanly (Q74 + Q69). `claude_code_task` field renamed and deleted. No transition periods.
- **No push.** Local commits only.
- **Standard footer:**
  ```
  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  ```
- **If a fix turns out larger than expected or you hit a blocker** — stop, commit the partial work with a clear status comment, document the blocker in your hand-back. Don't half-finish; don't bypass.

### Execution order

Follow the document's "Implementation order" section literally, but with **D13 + D7 promoted to land before D1**:

1. **D13** — Q74 gate-2 reject FSM-control-flow fix. **CRITICAL — lands FIRST.** Without this, gate-2 user veto is non-functional and the rest of v2.2.7's UX fixes are built on a broken foundation. UserAnswer shape extension + gateStep resume rewrite + FINALIZE.run null-verdict throw + 5-7 FSM tests. ~4-6h.
2. **D7** — Q68 acceptance gate on impl-blockers. HIGH severity. Independent of D13. Prompt edit (`agents/acceptance.md`) + new `INV_013` in `mcp/src/lib/invariants.ts` + 3-4 tests. ~3-4h.
3. **D1** — Classifier-agent auto-spawn. Biggest single block; FSM-test rework is the marathon. Write `mockClassifierSpawnProvider()` helper FIRST, validate on 2-3 tests, then bulk-apply. ~1.5-2 days.
4. **D2** — Reviewer selectivity by change_kind. Depends on D1 (classifier must run to emit change_kind). ~1 day.
5. **D3** — Implementer tech-debt auto-capture. Independent. ~3-4h.
6. **D4** — Generic shuttle response shape Q65 (`claude_code_task` → `spawn_request`). Pure type refactor; skill markdown landing in the same commit. ~1 day.
7. **D5** — Configurable project subdirectory Q66. Smallest, independent. ~2-3h.
8. **D6** — Parallelize planning-phase reviewers Q67. Mirrors impl REVIEW fan-out pattern. ~3-4h.
9. **D8** — Q69 auto-derived gate feedback. Depends on D6 (fan-out shape) and partially on D1 (`task_short`). ~4-6h.
10. **D9** — Q70 auto-replan loop (opt-in). Depends on D8. ~3-4h.
11. **D10** — Q71 gate message truncation. Depends on D1 (`task_short`). ~1h.
12. **D11** — Q72 owner_id env-var docs + audit warning. Independent. ~2-3h.
13. **D12** — Q73 summary_line cap. Independent. ~1-2h.

After D13 + D7 + D1, the core safety + classifier substrate is sound. D2-D6 are scope expansions. D8-D9 are UX wins. D10-D12 are polish. Each one is testable in isolation.

### Hand-back

Print a summary:
- 13 commit SHAs (or N < 13 if any blocked, with explanation).
- Test count before / after (baseline 485).
- Files changed / added / removed counts.
- Q-items closed (Q65, Q66, Q67, Q68, Q69, Q70, Q71, Q72, Q73, Q74; plus Item 1 / Item 2 / Item 3 architectural shifts with no Q-number).
- Any NEW Q-items surfaced during execution — file in `specs/open-backlog.md` with next available number (currently **Q75**).
- Update `specs/open-backlog.md` to move Q65-Q74 rows into `specs/closed-q-items.md` under new "v2.2.7-classifier-portability" section, each with commit SHA.
- Update `specs/closed-q-items.md` Summary counts (46 → 56 Q-items; 6 → 7 bundles).
- Roadmap status — confirm `specs/phases/v2.2.7-classifier-portability.md` acceptance criteria all ticked. If anything slipped, note in the hand-back.
- Validation entry — new `validation/closed-tasks/<date>-v2.2.7-shipped.md` summarising what shipped + recommended next-step (real-task validation on 2-3 different change_kinds before starting v2.3 daemon).
- After the commit dance: archive the launcher prompt — `git mv specs/v2.2.7-classifier-portability.prompt.md specs/done/` + update `specs/done/README.md` table (only after PR merges to main, not before).
- Recommendation for human's next step:
  - Real-task validation: run `/task` on 2-3 different change_kinds — confirm reviewer-skipped audits emit for type-only diffs, classifier auto-spawn fires every task, gate-2 reject-revise actually loops back to impl phase, gate-2 reject-abandon produces metric row with `verdict: "rejected"`.
  - After validation: v2.3-daemon-webui (next phase per `specs/phases/v2.3-daemon-webui.md`).

End the session. Do NOT run `/task` from this session — human will start fresh session for real-task validation post-merge.

## PROMPT END
