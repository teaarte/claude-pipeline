# 2026-05-18 — v2.2.6 stack-classifier + critical workflow fixes shipped

Branch: `v2.2.6-stack-classifier` off `main` (8 commits, local only — no push).

## What shipped

8 commits, C1-C8, per `specs/phases/v2.2.6-stack-classifier.md`. Stack-detect
architectural shift (Category 1 → Category 3 per the doctrine in
`product-vision.md`) + three workflow-critical fixes surfaced by the
frontend-core 2026-05-18 real-task run (Items 6-8 / Q63 / Q64).

| Group | SHA | Subject |
|---|---|---|
| C1 | `db6c754` | docs(v2.2.6): reclassify stack-detect Category 1→3 + knowledge-store P-K sketch |
| C2 | `1fbbeaf` | feat(v2.2.6 C2): stack-candidates.yaml + Zod-validated loader |
| C3 | `ec41eb0` | feat(v2.2.6 C3): rewrite stack-detect as candidate-driven (zero per-language branches) |
| C4 | `d5b8644` | feat(v2.2.6 C4): extend classifier-output schema with stack + change_kind |
| C5 | `f7d3eb6` | feat(v2.2.6 C5): <!-- validation-commands --> marker convention |
| C6 | `52abb3f` | feat(v2.2.6 C6): canonical task_id propagation + defensive rewrite |
| C7 | `38f35a8` | feat(v2.2.6 C7): auto-close validation + final on clean success (Q63) |
| C8 | `8398764` | feat(v2.2.6 C8): cross-session ownership safety (Q64) |

## Acceptance metrics

- **Tests:** 417 → 485 (+68 new). Target was baseline + ≥ 15. **Exceeded by 53.** All green per commit.
- **Test files:** 55 → 60 (+5: `stack-candidates.test.ts`, `classifier-output-schema.test.ts`, `owner.test.ts`, plus extensions to 12 existing test files).
- **Typecheck:** clean per commit.
- **Smoke (`pnpm smoke` + `pnpm smoke:orchestrator`):** green per commit.
- **Grep purity gate over `mcp/src/driver/core/`:** clean (zero plugin-name leaks) on every commit.
- **CC-binding scope:** `hooks/pipeline-stop.sh` is the only file reading CC's `session_id` directly from the payload. `lib/owner.ts` + `run-task.ts` reference `CLAUDE_SESSION_ID` env-var name as one of three candidate sources in a generic chain — pipeline core never interprets the value, just compares opaque strings.
- **Net diff:** 45 files changed, +2261 / -203 lines.
  - Added: 6 files (`templates/stack-candidates.yaml`, `mcp/src/lib/stack-candidates.ts`, `mcp/src/lib/owner.ts`, 3 new test files).
  - Modified: 39 files.

## Acceptance criteria (from spec)

**Original scope (stack-classifier):**
- ✓ `templates/stack-candidates.yaml` exists with 9 languages (TS, JS, Python, Rust, Go, C#, Svelte, Elixir, Dart) — exceeds the ≥ 8 minimum.
- ✓ Adding a new language is YAML-only — proven by the in-test Crystal fixture in `stack-detect.test.ts` (resolveStack picks `crystal` via `parseStackCandidatesString`).
- ✓ `stack-detect.ts` contains zero per-language conditional branches — `pickLanguage`, `pickPackageManager`, `pickProjectType`, `pickCommand` all iterate the candidate registry.
- ✓ `classifier-output.schema.json` validates `stack` AND `change_kind` (13 dedicated tests; `agents/classifier.md` template documents both — auto-spawn is deferred to v2.2.7 Item 1 per spec).
- ✓ `<!-- validation-commands -->` marker parsed; English header retained as deprecated fallback.
- ✓ Project without CLAUDE.md still gets sensible stack detection (covered by new `C5: project WITHOUT CLAUDE.md still gets a sensible stack via candidates` test).
- ✓ `product-vision.md:257` reclassifies stack-detect as Category 3 + audit obligation paragraph added.
- ✓ `specs/phases/far-future.md` describes Phase P-K (Knowledge-as-data) future direction.

**Workflow-critical fixes (Items 6-8):**
- ✓ Spawn-context Canonical identifiers section lives in `shuttle-provider.buildPrompt` BEFORE the role template (verified by ordering test). 13 reviewer/validator agent templates carry the constraint. Defensive rewrite in `record_agent_run` with audit class `task_id-rewrite`.
- ✓ Clean-success runs no longer write `pipeline_violation: "phase-force-final"`. Q63 auto-close fires only when verdict=accepted + no force + all prior phases closed + final=pending. Genuine Q54 recovery (`set_phase_status final force=true`) preserves the violation marker.
- ✓ `state.owner_id` populated from generic env-var chain; Stop hook compares CC session_id to owner_id; mismatch → INFO + clean exit (does NOT block + does NOT suggest `/done`). `pipeline_finish` and `pipeline_abandon` refuse `OWNER_MISMATCH` cross-session calls; `force_cross_owner: true` overrides and audits.

## What's NEW that wasn't in the spec

Nothing material — all 8 commits track the spec's implementation-order section literally. One small adjustment:

- The YAML's command-shape table was tightened to preserve test parity with v2.2.5 (`npm run X` for npm/bun, scripts-gating for TS/JS, `pip` PM as fallback for pyproject-only Python projects). All adjustments documented in C3 commit body.

## Q-items closed

- **Q63** (🟡 MEDIUM) — `pipeline_violation: "phase-force-final"` on every successful run. Closed by C7 (`38f35a8`). Filed in `closed-q-items.md` under "v2.2.6-stack-classifier".
- **Q64** (🟡 MEDIUM) — Cross-session false-positive in `pipeline-stop.sh`. Closed by C8 (`8398764`). Filed in `closed-q-items.md` under "v2.2.6-stack-classifier".

Items 1-5 (stack-classifier substrate) are architectural shifts rather than Q-items; documented under "v2.2.6-stack-classifier" in `closed-q-items.md` with `(architecture)` Q-name and the relevant commit SHAs.

Item 6 (canonical task_id) had no Q-number but is filed under the same section.

## New Q-items filed

None this session. Q67 remains the next available number — confirmed by reading `open-backlog.md` after the C7/C8 cleanup.

## Roadmap status

`specs/phases/v2.2.6-stack-classifier.md` acceptance criteria fully ticked (see above). No slippage. v2.2.7 unblocked.

## Boundary notes

1. **CC-binding scope.** `grep -rE "CLAUDE_SESSION_ID|claude.code.task" mcp/src/` matches:
   - `mcp/src/driver/tools/run-task.ts` — `CLAUDE_SESSION_ID` named in the env-var chain (matches spec body's reference implementation at lines 319-324 of `v2.2.6-stack-classifier.md`).
   - `mcp/src/lib/owner.ts` — same name listed in `OWNER_ID_ENV_VARS` precedence array.
   - `mcp/src/driver/types/shuttle.ts` + `core/shuttle.ts` + `bundles/code/steps/index.ts` — `claude_code_task` field name. This is the rename target of v2.2.7 Item 4 / Q65; left in place per the launcher's "shipping order" intent.

   Interpretation: pipeline core never **interprets** these values — they're opaque strings read by name. The launcher's stricter "zero matches" gate would require either (a) a wrapper script for CC users that maps `CLAUDE_SESSION_ID` → `CLAUDE_PIPELINE_OWNER_ID` before MCP server start, OR (b) waiting for the v2.2.7 Q65 rename to clean up `claude_code_task`. Both are downstream of this bundle.

2. **Schema version.** `pipeline-state.schema.json` stays at `1.1` (no bump). The new `owner_id` field is optional (`["string", "null"]`), so existing 1.1 states without it remain valid.

3. **No backward-compat shims.** Per `feedback_no_backcompat.md` memory: old per-language regex in `stack-detect.ts` was deleted, not deprecated. `record-agent-run`'s `header.task_id` is silently rewritten to canonical (not "warned then rewritten"). `pipeline-state` template adds `owner_id: null` directly with no migration.

## Recommendation for next session

**Real-task validation BEFORE starting v2.2.7.** Run `/task` on 2-3 different `change_kind` shapes against `OSC/frontend-core` or `wandr-be`. Confirm:

1. Findings carry the canonical `task_id` (`t-YYYY-MM-DD-slug` format) — Item 6 in production.
2. Clean accepted runs produce metric row with `force_used: false` + `pipeline_violation: null` — Q63 in production.
3. Cross-session test: open two CC windows in the same project; run `/task` in window A; close window B → Stop hook emits the INFO message + lets B close — Q64 in production.

Suggested change-kind mix:
- **type-only** (e.g. small TS interface widening) — exercises C3 + the implicit assumption that style + performance reviewers will be skip-candidates in v2.2.7.
- **logic** (a small bug-fix) — full reviewer fan-out + acceptance:PASS path.
- **ui** (a styling tweak in a React component) — exercises ui-consistency + the Q63 auto-close on a more typical task shape.

After the real-task pass, start v2.2.7 fresh (`v2.2.7-classifier-portability`) per the launcher in `specs/phases/v2.2.7-classifier-portability.md`. D1 (classifier auto-spawn) is the biggest single commit and should land first — it depends on C6 (task_id) which already shipped here.

Do NOT run `/task` from THIS session — start fresh so context isn't biased toward the changes that just landed.
