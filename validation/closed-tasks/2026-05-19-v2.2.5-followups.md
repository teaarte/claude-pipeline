# 2026-05-19 — v2.2.5-followups bundle

Branch: `v2.2.5-bundle-foundation` (followups landed directly on the foundation
branch per session direction).

## What shipped

10 commits, C1-C10, covering 13 HIGH + 16 MEDIUM + 6 LOW items surfaced by
the full-codebase code review post-v2.2.5 launcher.

| Group | SHA | Subject |
|---|---|---|
| (spec) | `b23fa23` | docs(spec): v2.2.5 follow-ups punch list + launcher prompt |
| C1 | `0230295` | fix(driver): item 8 zod regression — accept structured user-answer |
| C2 | `f4f9c6b` | fix(driver): item 7 — propagate team_knowledge across REVIEW fan-out |
| C3 | `f39259e` | fix(stack-detect): thread package_manager into command synthesis |
| C4 | `34c57ae` | fix(agents): finding IDs + refs frontmatter + reviewer-prompt rule |
| C5 | `6ea1bdd` | fix(guard): inspect bash -c body before flagging as write-op |
| C6 | `fc7baad` | fix(mcp-clients): error handler + timeout cleanup for child processes |
| C7 | `4e7ce93` | fix(state-io): shared feedback lock + always-lock audit + corrupt-state detection |
| C8 | `5b243da` | fix(fix-task-id): atomically rewrite findings.jsonl task_id |
| C9 | `541197f` | chore(drift): C9 — M1-M10 + M13-M16 cleanup |
| C10 | `48433b5` | chore(low): C10 — L1-L5 low-severity items |

## Acceptance metrics

- **Tests:** 396 → 417 (+21). Target was ≥ 416. All green per commit.
- **Typecheck:** clean per commit.
- **Smoke (`pnpm smoke` + `pnpm smoke:orchestrator`):** green per commit.
- **Grep-purity gate:** `mcp/src/driver/core/` contains zero plugin-name
  references throughout.
- **Files:** 63 changed (+1 added not counted in diff stat — `tool-registry.ts`).
  6 new files, 57 modified.
- **Net diff:** +1477 / -198 lines.

### High items closed (13/13)

| Item | Commit | Test added |
|---|---|---|
| H1 — continue-task user-answer zod schema | C1 | continue-task-schema.test.ts (4 tests) |
| H2 — team_knowledge dropped on REVIEW fan-out | C2 | q9-review-decisions.test.ts (+1) |
| H3 — stack-detect hardcoded `npm run` | C3 | stack-detect.test.ts (+2) |
| H4 — Python project_type copy-paste bug | C3 | stack-detect.test.ts (+1) |
| H5 — 4-char finding IDs in 4 agent templates | C4 | grep-purity verification |
| H6 — refs frontmatter `security-reviewer`/`performance-reviewer` | C4 | grep-purity verification |
| H7 — guard.sh `bash -c` false positive | C5 | guard-evasion.test.ts (+4) |
| H8 — mcp-client-manager unhandled `error` event | C6 | mcp-client-manager.test.ts (+1) |
| H9 — withTimeout leaks orphan child | C6 | mcp-client-manager.test.ts (+1) |
| H10 — pattern-confidence ↔ log-agent-feedback lock | C7 | set-pattern-confidence-h10.test.ts (+1) |
| H11 — audit appendCapped fast-path race | C7 | audit.test.ts (+1) |
| H12 — withStateLock silent null on empty `{}` | C7 | state-io-h12.test.ts (+4) |
| H13 — fix-task-id doesn't update findings.jsonl | C8 | fix-task-id.test.ts (+1) |

### Medium items closed (16/16)

M1-M10 + M13-M16 — see `541197f` commit body for itemised notes.
M11 (classifier cap mismatch) + M12 (refs section-name rule) shipped with C4.

### Low items closed (6/6)

L1-L5 in C10. L6 (playwright title) in C4.

## What slipped

Nothing. The full 13H + 16M + 6L set landed.

## New Q-items filed

- **Q62** (🟢 LOW) — Ref content enrichment phase. M12 dropped the
  reviewer-prompt rule that required `**Red Flags in Diff**` /
  `**Anti-Patterns**` sections in every ref. A future phase should decide
  whether structured sections, extended frontmatter, or per-stack
  expansion is the right shape. Recorded in `open-backlog.md`.

`open-backlog.md`'s "next: Q62" line updated to "next: Q63".

## Q-items closed

None — this follow-up bundle surfaced bugs that pre-dated the Q-item
ledger. Commits reference items by H/M/L code from the spec, not by
Q-number.

## Recommendation for next session

C1-C4 was the "before real-task v2.2.5 verification" critical gate; all
landed. The codebase is now safe to drive a real task through.

**Suggested next step:** real-task verification of v2.2.5 on either
**s3-panel** (frontend, Next.js + pnpm — exercises C3 stack-detect, C2
team_knowledge fan-out, C5 guard hardening for `bash -c` reads) or
**wandr-be** (backend, NestJS — exercises C1 user-answer protocol via
gate-0/1/2, C7 audit + state-io reliability).

s3-panel is the higher-coverage choice because Next.js triggers the
biggest agent fan-out (security + performance + ui-consistency on top of
logic/challenger/style). The frontend-core 2026-05-17 doc-drift run already
demonstrated the v2.2.5 spawn-pause path works on a Next.js project — this
verification should compare against that baseline.

Hold off on real-task validation **from the same session that did this
work**. Start fresh so context isn't biased toward the changes that just
shipped.
